# Fast amd light Python Framework for API build
fastapi == 0.65.1

# Fast ASGI server which helps in the interaction between app and the backend 
uvicorn == 0.14.0

# Motor MongoDB driver for Python 
motor == 2.4.0